--- a/lib/src/execution/options.ex
+++ b/lib/src/execution/options.ex
@@ -207,9 +207,9 @@
        |> Map.put(:cardinalities, cardinalities)
        |> Map.put(:graph_format, graph_format)
        |> Map.put(:palette, palette)
-       |> Map.put(:verbalization_backend, verbalization_backend)}
+       |> Map.put(:verbalization_backend, verbalization_backend)
        |> Map.put(:verbalization_mode, verbalization_mode)
-       |> Map.put(:verbalization_reasoning?, verbalization_reasoning?)
+       |> Map.put(:verbalization_reasoning?, verbalization_reasoning?)}
     end
   end
 
@@ -256,9 +256,13 @@
 
     cond do
       Regex.match?(~r/^\d+$/, value) ->
-        value
-        |> String.to_integer()
-        |> normalize_cardinalities()
+        cardinality = String.to_integer(value)
+
+        if cardinality > 0 do
+          {:ok, [cardinality]}
+        else
+          {:error, {:invalid_cardinality, value}}
+        end
 
       match = Regex.run(~r/^(\d+)-(\d+)$/, value) ->
         [_, first, last] = match
--- a/lib/src/execution/pipeline.ex
+++ b/lib/src/execution/pipeline.ex
@@ -192,7 +192,7 @@
         seed: Map.get(run.params, :verbalization_seed, 42),
         max_new_tokens: Map.get(run.params, :verbalization_max_new_tokens, 768),
         backend_options: Map.get(run.params, :verbalization_backend_options, %{}),
-        verbalization_mode: Map.get(run.params, :verbalization_mode, :grouded),
+        verbalization_mode: Map.get(run.params, :verbalization_mode, :grounded),
         reasoning: Map.get(run.params, :verbalization_reasoning?, false)
       ]
 
--- a/lib/src/interface/cli.ex
+++ b/lib/src/interface/cli.ex
@@ -140,6 +140,12 @@
                                               local or hpc_connect.
                                               Default: local.
 
+      --cardinality N|FIRST-LAST              World cardinality or inclusive range.
+                                              Default: 2.
+
+      --cardinality-feature                   Include world cardinality as an explicit
+                                              graph-analysis feature.
+
       --verbalization-mode MODE               grounded or interpretive.
                                               Default: grounded.
 
@@ -387,7 +393,7 @@
       {:error, {:invalid_cardinality, invalid}} ->
         {:error,
           "Invalid cardinality #{inspect(invalid)}." <>
-            "Use a postive integer or inclusive range such as 2 or 2-4."}
+            "Use a positive integer or inclusive range such as 2 or 2-4."}
       {:error, reason} ->
          {:error, "Invalid cardinality option: #{inspect(reason)}"}
     end
--- a/graph_ml/launcher.py
+++ b/graph_ml/launcher.py
@@ -86,19 +86,23 @@
     graphs = []
 
     for graph_index, model_path, metadata, graph in parsed_models:
-        graph.graph["model_id"] = _analysis_model_id(metadata, model_path, multi_cardinality)
+        graph.graph["model_id"] = _analysis_model_id(
+            metadata,
+            model_path,
+            multi_cardinality=multi_cardinality,
+        )
         graph.graph["graph_index"] = graph_index
         graphs.append(graph)
         
     occurrences_by_graph = [pattern_occurrences(graph, size=graphlet_size) for graph in graphs]        
     vectors = [
-        feature_vector(
-            graph, 
-            method=feature_method, 
-            graphlet_size=graphlet_size, 
+        _analysis_feature_vector(
+            graph,
+            feature_method=feature_method,
+            graphlet_size=graphlet_size,
             graphlet_occurrences=occurrences_by_graph[graph_index],
-            include_cardinality_feature=include_cardinality_feature
-        ) 
+            include_cardinality_feature=include_cardinality_feature,
+        )
         for graph_index, graph in enumerate(graphs)
     ]
     matrix = feature_matrix(vectors)
@@ -147,8 +151,21 @@
 
     return f"model-{int(iteration):03d}"
 
-def _analysis__feature_vector(graph, *, feature_method: str, graphlet_size: int, graphlet_occurrences, include_cardinality_feature: bool):
-    vector = feature_vector(graph, method=feature_method, graphlet_size=graphlet_size, graphlet_occurrences=graphlet_occurrences)
+def _analysis_feature_vector(
+    graph,
+    *,
+    feature_method: str,
+    graphlet_size: int,
+    graphlet_occurrences,
+    include_cardinality_feature: bool,
+):
+    vector = feature_vector(
+        graph,
+        method=feature_method,
+        graphlet_size=graphlet_size,
+        graphlet_occurrences=graphlet_occurrences,
+    )
+
     if not include_cardinality_feature:
         vector.pop("worlds", None)
 
@@ -229,4 +246,4 @@
     }
     
 if __name__ == "__main__":
-    raise SystemExit(main())
\ No newline at end of file
+    raise SystemExit(main())
--- a/verbalization/prompt.py
+++ b/verbalization/prompt.py
@@ -73,8 +73,8 @@
 instructions.
 
 Explain the semantic tension exhibited by the recorded bounded countermodels
-and their graphlet patterns. Explain each graphlet through its local Kripke
-semantics: worlds, valuations, accessibility relations, explicitly absent
+and their graphlet patterns. Explain each graphlet through its local Kripke semantics:
+worlds, valuations, accessibility relations, explicitly absent
 relation cells, and the designated world where supplied.
 
 Explain how an applied refinement axiom strengthens the preceding theory as an
@@ -265,4 +265,4 @@
             f"Expected one of: {supported}."
         )
 
-    return normalized
\ No newline at end of file
+    return normalized
--- a/test/python/test_graph_launcher.py
+++ b/test/python/test_graph_launcher.py
@@ -78,7 +78,13 @@
 
 def test_analysis_feature_vector_excludes_cardinality_by_default() -> None:
     graph = nx.DiGraph()
-    graph.add_nodes_from([0, 1, 2])
+    graph.add_nodes_from(
+        [
+            (0, {"designated": True}),
+            (1, {"designated": False}),
+            (2, {"designated": False}),
+        ]
+    )
 
     without_cardinality = _analysis_feature_vector(
         graph,
--- a/test/src/execution/options_test.exs
+++ b/test/src/execution/options_test.exs
@@ -30,7 +30,7 @@
              Options.new(
                model_logic: " DDL ",
                graph_format: "TikZ",
-               max_parallel_renders: 2,
+               max_parallel_renderers: 2,
                palette: "Magma",
                cardinality: "2-4",
                verbalization_mode: " Interpretive ",
@@ -40,6 +40,7 @@
     assert options.model_logic == :ddl
     assert options.graph_format == :tikz
     assert options.palette == :magma
+    assert options.max_parallel_renderers == 2
     assert options.cardinalities == [2, 3, 4]
     assert options.verbalization_mode == :interpretive
     assert options.verbalization_reasoning?
--- a/test/src/execution/pipeline_test.exs
+++ b/test/src/execution/pipeline_test.exs
@@ -126,7 +126,7 @@
         cat > "$output" <<'JSON'
         {"schema":"modal-lens/analysis-report","schema_version":"1.1","analysis":{"model_count":1,"cluster_count":1,"reported_pattern_count":0,"refinement_candidate_count":0,"signature":{"atoms":[],"relation":"R"}},"refinement_candidates":[],"clusters":[{"cluster_id":0,"model_count":1,"model_fraction":1.0,"model_indices":[0],"characteristic_patterns":[],"representative_model":{"graph_index":0}}],"highlights":[{"graph_index":0,"model_id":"model-001","cluster_id":0,"highlight":null}]}
         JSON
-        printf '{"schema":"modal-lens/graph-analysis-result","schema_version":"1.0","status":"completed","report_path":"%s","model_count":1,"cluster_count":1}\n' "$output"
+        printf '{"schema":"modal-lens/graph-analysis-result","schema_version":"1.0","status":"completed","report_path":"%s","model_count":1,"cluster_count":1,"include_cardinality_feature":true}\n' "$output"
         """
       )
 
--- a/test/src/isabelle/hpc_connect_test.exs
+++ b/test/src/isabelle/hpc_connect_test.exs
@@ -121,9 +121,10 @@
 
     batch_script = decode_batch_script(job_command)
     assert batch_script =~ "module load isabelle"
-    assert batch_script =~ "exec 'isabelle' build"
+    assert batch_script =~ "exec 'isabelle' process_theories"
+    assert batch_script =~ "-l 'HOL'"
     assert batch_script =~ "-o 'threads=6'"
-    assert batch_script =~ "'Remote'"
+    assert batch_script =~ "-f 'Remote.thy'"
 
     assert_receive {:run_command, %Command{} = job_ssh_command, job_run_opts}
     assert job_ssh_command.remote_command == job_command
